package:
  name: conda-build-test
  version: 1

build:
  number: 0
  noarch: generic  # [not win]
  script: '"{{ PYTHON }}" "{{ RECIPE_DIR }}/run_test.py"'

requirements:
  host:
    - python
  run:
    - python
